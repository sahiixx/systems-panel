---
import Layout from '../../layouts/Layout.astro';
import { loadSystemsData, getModuleById, getSummary, getAllModuleIds } from '../../utils/systems.ts';
import { formatDate, formatMetricKey } from '../../utils/systems.ts';
import StageBadge from '../../components/StageBadge.astro';
import HealthIndicator from '../../components/HealthIndicator.astro';

export async function getStaticPaths() {
  const data = await loadSystemsData();
  return data.modules.map(m => ({ params: { id: m.id } }));
}

const { id } = Astro.params;
const data = await loadSystemsData();
const module = getModuleById(data, id as string);

if (!module) {
  return Astro.redirect('/404');
}

const summary = getSummary(data);
const domainSlug = module.domain.toLowerCase().replace(/\s+/g, '-').replace(/&/g, '');
---

<Layout title={`${module.name} — SAHIIXX Systems Panel`} description={module.description}>
  <main class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <nav class="mb-6" aria-label="Breadcrumb">
      <ol class="flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400">
        <li><a href="/" class="hover:underline">Systems Panel</a></li>
        <li aria-hidden="true">/</li>
        <li aria-current="page">{module.name}</li>
      </ol>
    </nav>
    
    <article class="space-y-8">
      <header>
        <div class="flex items-center gap-3 mb-4 flex-wrap">
          <h1 class="text-3xl font-bold text-gray-900 dark:text-white">{module.name}</h1>
          <StageBadge stage={module.stage} />
          <HealthIndicator health={module.health} />
        </div>
        <p class="text-lg text-gray-600 dark:text-gray-400 max-w-2xl">{module.description}</p>
        <div class="flex flex-wrap gap-2 mt-4">
          {module.tags.map(tag => (
            <span class="px-3 py-1 text-sm bg-gray-100 dark:bg-gray-800 rounded-full text-gray-700 dark:text-gray-300">{tag}</span>
          ))}
        </div>
      </header>
      
      <section>
        <h2 class="text-xl font-semibold mb-4">Metrics</h2>
        <dl class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          {Object.entries(module.metrics).map(([key, value]) => (
            <div class="bg-white dark:bg-gray-900 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
              <dt class="text-sm text-gray-500 dark:text-gray-400 mb-1">{formatMetricKey(key)}</dt>
              <dd class="font-mono font-medium text-lg text-gray-900 dark:text-white">{value}</dd>
            </div>
          ))}
        </dl>
      </section>
      
      <section>
        <h2 class="text-xl font-semibold mb-4">Links</h2>
        <div class="flex flex-wrap gap-3">
          <a href={module.repo} target="_blank" rel="noopener" class="btn-secondary flex items-center gap-2">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0112 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/></svg>
            GitHub
          </a>
          {module.deployUrl && (
            <a href={module.deployUrl} target="_blank" rel="noopener" class="btn-primary flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/></svg>
              Live Deploy
            </a>
          )}
          {module.dashboardUrl && (
            <a href={module.dashboardUrl} target="_blank" rel="noopener" class="btn-secondary flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"/></svg>
              Dashboard
            </a>
          )}
          {module.mcpUrl && (
            <a href={module.mcpUrl} target="_blank" rel="noopener" class="btn-secondary flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2zM9 9h6v6H9V9z"/></svg>
              MCP Server
            </a>
          )}
          {module.docsUrl && (
            <a href={module.docsUrl} target="_blank" rel="noopener" class="btn-secondary flex items-center gap-2">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"/></svg>
              Docs
            </a>
          )}
        </div>
      </section>
      
      <section>
        <h2 class="text-xl font-semibold mb-4">Deploy History</h2>
        <p class="text-gray-600 dark:text-gray-400">Last deploy: <time class="font-mono">{formatDate(module.lastDeploy)}</time></p>
        {module.links?.monitoring && (
          <a href={module.links.monitoring} target="_blank" rel="noopener" class="text-primary-600 dark:text-primary-400 hover:underline mt-2 inline-block">
            View Grafana Dashboard →
          </a>
        )}
      </section>
      
      <section>
        <h2 class="text-xl font-semibold mb-4">Architecture</h2>
        <div class="bg-gray-50 dark:bg-gray-800 rounded-lg p-6 font-mono text-sm overflow-x-auto">
          <pre>{`flowchart LR
  A[GitHub] --> B[Registry]
  B --> C[Router]
  C --> D[Runner]
  D --> E[MCP/CLI/API]
  E --> F[Dashboard]
  B --> G[Approval Gates]
  D --> H[Clearwing Sandbox]
  D --> I[Observability (OTel → Cloudflare)]
  H --> J[Audit Log (Immutable)]`}</pre>
        </div>
      </section>
    </article>
  </main>
</Layout>

<style define:vars={{ primary: '#0ea5e9' }}>
  .btn-primary {
    @apply inline-flex items-center gap-2 px-4 py-2 bg-primary-600 text-white rounded-lg hover:bg-primary-700 transition-colors text-sm font-medium;
  }
  .btn-secondary {
    @apply inline-flex items-center gap-2 px-4 py-2 bg-gray-100 dark:bg-gray-800 text-gray-700 dark:text-gray-300 rounded-lg hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors text-sm font-medium;
  }
</style>