---
import Layout from '../layouts/Layout.astro';
import { getModules, getSummary, loadSystemsData } from '../utils/systems.ts';
import SummaryStats from '../components/SummaryStats.astro';
import MetricsStrip from '../components/MetricsStrip.jsx';
import DomainFilter from '../components/DomainFilter.astro';
import ModuleCard from '../components/ModuleCard.astro';
import LastUpdated from '../components/LastUpdated.astro';

const systemsData = await loadSystemsData();
const modules = getModules(systemsData);
const summary = getSummary(systemsData);
const isInvestorView = Astro.url.searchParams.get('investor') === '1';
---

<Layout title="SAHIIXX Systems Panel" description="Live status of all SAHIIXX modules — kernel, verticals, and experiments">
  <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <header class="mb-8">
      <div class="flex items-center justify-between flex-wrap gap-4 mb-6">
        <div>
          <h1 class="text-3xl font-bold text-gray-900 dark:text-white">Systems Panel</h1>
          <p class="text-gray-600 dark:text-gray-400 mt-1">Live status of all SAHIIXX modules — kernel, verticals, and experiments</p>
        </div>
        <div class="flex items-center gap-3">
          <a href="https://github.com/sahiixx/systems-panel" target="_blank" rel="noopener" class="text-sm text-gray-500 hover:text-gray-700 dark:hover:text-gray-300 flex items-center gap-1">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0C5.374 0 0 5.373 0 12c0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0112 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/></svg>
            View Source
          </a>
          <LastUpdated lastUpdated={systemsData.lastUpdated} />
        </div>
      </div>
      
      {isInvestorView && (
        <div class="fixed top-0 left-0 right-0 bg-amber-100 dark:bg-amber-900 text-amber-900 dark:text-amber-100 px-4 py-2 text-center text-sm font-medium border-b border-amber-300 dark:border-amber-700 z-50">
          🔒 INVESTOR VIEW — CONFIDENTIAL — SAHIIXX Pre-Seed
        </div>
      )}
    </header>
    
    <MetricsStrip client:visible initialMetrics={summary.aggregateMetrics || {}} />
    <SummaryStats summary={summary} />
    <DomainFilter />
    
    <div class="module-grid grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4" id="modules-grid">
      {modules.map(module => (
        <ModuleCard 
          module={module} 
          class:list="module-card"
          data-domain={module.domain.toLowerCase().replace(/\s+/g, '-')}
        />
      ))}
    </div>
    
    {modules.length === 0 && (
      <div class="text-center py-12 text-gray-500 dark:text-gray-400">
        No modules match the current filter.
      </div>
    )}
    
    <div class="mt-12 pt-8 border-t border-gray-200 dark:border-gray-800">
      <h2 class="text-xl font-semibold text-gray-900 dark:text-white mb-4">Summary by Domain</h2>
      <dl class="grid grid-cols-2 md:grid-cols-4 gap-4 text-sm">
        {Object.entries(summary.byDomain).map(([domain, count]) => (
          <div key={domain} class="bg-white dark:bg-gray-900 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
            <dt class="text-gray-500 dark:text-gray-400 mb-1">{domain}</dt>
            <dd class="font-bold text-gray-900 dark:text-white text-xl">{count}</dd>
          </div>
        ))}
      </dl>
    </div>
    
    <div class="mt-8 pt-8 border-t border-gray-200 dark:border-gray-800">
      <h2 class="text-xl font-semibold text-gray-900 dark:text-white mb-4">Summary by Stage</h2>
      <dl class="grid grid-cols-2 md:grid-cols-5 gap-4 text-sm">
        {Object.entries(summary.byStage).map(([stage, count]) => (
          <div key={stage} class="bg-white dark:bg-gray-900 rounded-lg border border-gray-200 dark:border-gray-700 p-4">
            <dt class="text-gray-500 dark:text-gray-400 mb-1 capitalize">{stage}</dt>
            <dd class="font-bold text-gray-900 dark:text-white text-xl">{count}</dd>
          </div>
        ))}
      </dl>
    </div>
  </main>
</Layout>